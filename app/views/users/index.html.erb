<% @users.each do |user| %>
  <strong>email:</strong>
  <%= user.email %>
  <strong>current_sign_in_ip:</strong>
  <%= user.current_sign_in_ip %>
  <strong>confirmed?</strong>
  <%= user.confirmed? %>
  <br>
<% end %>